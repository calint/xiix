#pragma once
#include<stdio.h>
namespace xiix{
namespace meters{
	size_t events{0};
	size_t input{0};
	size_t output{0};
	size_t requests{0};
	size_t opens{0};
	size_t reads{0};
	size_t writes{0};
	size_t brks{0};
	size_t errors{0};
}
}
